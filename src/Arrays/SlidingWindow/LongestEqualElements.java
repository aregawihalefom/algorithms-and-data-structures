package Arrays.SlidingWindow;

/**
 *
 * Description : Write a program that takes an array of integers and finds the length of a
 *               longest subarray all of whose entries are equal.
 *
 * Approach  : Use sliding window to keep track of the longest subarray with equal elements
 *
 */
public class LongestEqualElements {
}
